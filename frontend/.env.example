# URL base da API do backend (Flask). Em dev local, deixe em branco para usar
# o proxy do Vite (vite.config.js). Em produção (Vercel), aponte para a URL
# pública do backend hospedado no Render.
VITE_API_URL=
